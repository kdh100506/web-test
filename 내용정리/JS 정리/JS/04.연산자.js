num=10;
num+=5
console.log(num)

num=10;
num++; // 증가 연산자 (1씩 증가)
console.log(num)

num=10
num--; // 감소 연산자 (1씩 감소)
console.log(num)

num=10;
result=num++; // ++를 뒤에 넣으면 증가 시키기 전의 값을 result에 넣음
console.log(result)

num=10;
result=++num; // ++를 앞에 넣으면 증가 시킨 후의 값을 result에 넣음
console.log(result)

